# Candidate evaluation archive, suite 7

This hash-verified archive preserves the original evaluation manifest, raw trial summary, original and corrected scorecards, and classification correction byte-for-byte. Verify it with `sha256sum -c evidence.tar.gz.sha256`; extract it in a temporary directory to inspect individual records.

The 24 candidate slots ran against the frozen Linux profiles. Every trial encountered provider rate limiting before a usable model turn. The corrected score is **F / 0.0** for both hosts, with all 240 rubric observations unverified. The original scorecard and trial records are retained in the archive; the correction reclassifies the slots as infrastructure failures and does not infer product outcomes.
