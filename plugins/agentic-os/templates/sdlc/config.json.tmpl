{
  "schema": 1,
  "mode_defaults": {
    "autonomous": {
      "escalate_on": [{{ESCALATE_ON}}],
      "max_clarifying_questions_per_phase": 3
    }
  },
  "context_boundaries": "suggest",
  "model_tiers": {
    "economy": "inherit",
    "standard": "inherit",
    "premium": "inherit"
  },
  "feature_verification": {
    "allow_dynamic_playwright": true,
    "app_start_command": "{{APP_START_COMMAND}}",
    "base_url": "{{BASE_URL}}"
  },
  "integrations": {
    "ticket": {
      "enabled": true,
      "adapter": "documented in .agentic/guides/project.md ({{TICKET_ADAPTER}})"
    },
    "github": { "enabled": true, "command": "{{MR_ADAPTER}}" }
  }
}
